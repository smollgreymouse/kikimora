# Legacy Stage 0 path

The production managed-VPN runtime is now named **Toad**.

This file is retained only for compatibility with older links. Do not execute implementation work from this document.

Use:

1. `docs/toad-stage0.md` — canonical Stage 0 contract;
2. `docs/toad-roadmap.md` — living architecture/status roadmap;
3. `docs/toad-naming.md` — canonical names;
4. the active packet in `docs/toad-steps/` — executor-level instructions.

Canonical runtime root: `toad/`.
Canonical executable: `kikimora-toad`.
Per-instance service name: `kikimora-toad@<name>.service`.
